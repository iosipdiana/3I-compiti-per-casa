#include <stdlib.h>
#include <stdio.h>
#include <iostream>
using namespace std;
main()
{
    int i,j;
    int array[100];
    for(i=0;i<100;i++)
    array[i=1];
    for(i=2;i<100;i++)
    {
        if(array[i]==1)
        {
            for(j=1;j<(100/1)+1;j++)
                array[i*j]==0;
        }
        }
        cout<<"numeri primi <100"<<endl;
        for(i=1;i<100;i++)
        {
            if(array[1]!=0)cout< ? <<endl;
        }
        system("PAUSE");
    }

